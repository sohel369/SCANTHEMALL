{
  "name": "gtsa-frontend",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "npx serve frontend -l ${PORT:-3000} --no-clipboard",
    "build": "echo 'Static frontend - no build needed'"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
