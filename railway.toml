[build]
builder = "NIXPACKS"
buildCommand = "cd backend && npm install && cd ../admin-panel && npm install && npm run build"

[deploy]
startCommand = "cd backend && node src/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
